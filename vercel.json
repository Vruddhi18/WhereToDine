{
  "version": 2,
  "builds": [
    {
      "src": "Restar/assets/js/server.js",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["src/**"]
      }
    },
    {
      "src": "api/app.py",
      "use": "@vercel/uvicorn app:app --reload"
    }
  ],
  "routes": [
    {
      "src": "/Restar/(.*)",
      "dest": "Restar/app.py"
    },
    {
      "src": "/Restar/assets/js/(.*)",
      "dest": "Restar/assets/js/server.js",
      "methods": ["GET", "POST", "PUT", "DELETE", "PATCH", "OPTIONS"]
    }
  ]
}
